# Optimizer

Optimizer documentation

## Documentation

Store images in `img-optimizer/in` directory. Everything in that directory is treated as an image.

Then run the optimizer, it ignores the `.gitignore` in the input directory.

Run the optimizer:

```txt
npx txt imt-optimizer/optimizer.mts
```
